List <integer> numeros = new ArrayList<>();

numeros.add(30);
numeros.add(8);
numeros.add(5);
numeros.add(3);
numeros.add(14);
numeros.add(12);
numeros.add(21);
numeros.add(7);
int pares = 0;
int impares = 0;
int soma = 0;
int maior = numeros.get(0);
int menor = numeros.get(0);

For(int i = 0; i < numeros.size(); i++) {
soma += numeros.get(i);

If(numeros.get(i) % 2 == 0) {
pares++
} else {impares++
}
If(numeros.get(i) > maior) { maior = numeros.get(i);
}
If(numeros.get(i) < menor) { menor = numeros.get(i);
}
}
System.out.printIn("quantidade de pares: "+ pares);

System.out.printIn("quantidade de impares": + impares);

System.out.printIn("soma total: "+ soma);

System.out.println("maior numero: "+ maior);